# -- CARTRIDGE STUDIO ENVIRONMENT CONFIGURATION --------------------------------

# ScreenScraper API Developer Credentials (Optional but recommended)
# Register at: https://www.screenscraper.fr/
# NOTE: Never commit actual keys. Copy this file to '.env.local' and edit it there.
SCREENSCRAPER_DEV_ID=
SCREENSCRAPER_DEV_PASSWORD=
SCREENSCRAPER_SOFT_NAME=CartridgeStudio
# Server-side only (never exposed to the browser bundle). Base URL for
# 3D cartridge assets (e.g. your S3 bucket). Served to the client at
# runtime via /api/config. If unset, falls back to local /public paths.
THREE_D_BASE_URL=