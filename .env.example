VITE_SCREENSCRAPER_DEV_ID=
VITE_SCREENSCRAPER_DEV_PASSWORD=
VITE_SCREENSCRAPER_SOFT_NAME=
# Server-side only (never exposed to the browser bundle). Base URL for
# 3D cartridge assets (e.g. your S3 bucket). Served to the client at
# runtime via /api/config. If unset, falls back to the jsdelivr CDN.
THREE_D_BASE_URL=
