VITE_SCREENSCRAPER_DEV_ID=
VITE_SCREENSCRAPER_DEV_PASSWORD=
VITE_SCREENSCRAPER_SOFT_NAME=
# Server-only configuration. The value is returned at runtime by /api/config
# so the browser can load the 3D assets. Use an S3 bucket or Internet Archive item.
# Leave empty if no external asset host is configured.
# Example:
# THREE_D_BASE_URL=https://archive.org/download/7535476
THREE_D_BASE_URL=
